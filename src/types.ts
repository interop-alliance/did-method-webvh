/**
 * This file re-exports all types from interfaces.ts
 * It's used to provide a clear entry point for type imports
 *
 * @deprecated Import from the package root instead; this `./types` subpath is
 * slated for removal at the next major release.
 */

export * from './interfaces.js';
